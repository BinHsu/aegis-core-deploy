apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Workload identity declared via the platform's WorkloadIdentity XR
# (ADR-09 platform-as-product XRD). The platform's Crossplane Composition
# renders the underlying ACK Role; ACK reconciles it into AWS IAM.
resources:
  - aegis-core-engine-identity.yaml
