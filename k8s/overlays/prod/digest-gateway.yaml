# Gateway digest pin (ADR-10/14). JSON6902 patch FILE. Prod digests are
# PROMOTED from staging by a promotion PR that copies the staging-verified
# digest here (ADR-14 atomic promotion: gateway + engine move together or
# neither). Full static public GHCR ref (ghcr-graviton pivot). validate.yml
# enforces the @sha256 SHAPE and (on prod PRs) the move-together rule.
- op: replace
  path: /spec/template/spec/containers/0/image
  # TODO(ghcr-graviton #build-27872504892): hex below is the OLD amd64 ECR digest — operator REPLACES with the fresh linux/arm64 GHCR digest before prod apply
  value: ghcr.io/binhsu/aegis-core-gateway@sha256:5fb0df836dcc3c0c17a980527dd64357168a3955a76747e455799423e88acf35
