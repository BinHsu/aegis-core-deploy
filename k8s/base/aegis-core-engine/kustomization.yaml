apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Provider-neutral base (WS1). The kube-prometheus-stack ServiceMonitor moved
# to components/aws-binding (it assumes the Prometheus operator CRD, an
# AWS-cluster assumption here).
resources:
  - serviceaccount.yaml
  - rollout.yaml
  - service.yaml
  - networkpolicy.yaml
  - seed-job.yaml
